version: '3.8'

services:
  # Servicio de la aplicación Laravel
  laravel-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sistema-turnos-laravel
    ports:
      - "8080:80"
    volumes:
      - ./storage:/var/www/html/storage
    environment:
      - APP_ENV=local
      - APP_DEBUG=true
    restart: unless-stopped
    networks:
      - laravel-network

networks:
  laravel-network:
    driver: bridge
